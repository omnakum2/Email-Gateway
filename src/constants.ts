// Attachment rules (fixed code constants, not env-configurable).
export const ALLOWED_EXTENSIONS = ['pdf', 'csv', 'xlsx', 'docx', 'jpg', 'jpeg', 'png'];
export const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10 MB per file
export const MAX_ATTACHMENT_COUNT = 5;
